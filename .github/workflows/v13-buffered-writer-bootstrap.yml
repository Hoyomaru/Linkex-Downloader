name: validate-v13-buffered-writer

"on":
  push:
    branches:
      - exp/v1.3-buffered-writer
    paths:
      - '.github/workflows/v13-buffered-writer-bootstrap.yml'

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo workflow-ok
